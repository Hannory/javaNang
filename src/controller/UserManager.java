package controller;

import java.util.ArrayList;

import model.dao.AddUser;
import model.vo.User;

public class UserManager {
	/*private AddUser au = new AddUser();
	
	public void insertUser(User u) {
		int userNo = 0;
		
		ArrayList<User> list = au.readUserList();
		
		if(list == null) {
			
			list = new ArrayList<User>();
			
			userNo++;
		}else {
			userNo = list.get(list.size() - 1).getUserNo() + 1;
		}
		u.setUserNo(userNo);
		
		list.add(u);
		
		int result = au.writeUserList(list);
		
		//if(result > 0) {
	
	}*/

}
